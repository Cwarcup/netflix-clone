/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["images.unsplash.com", "i.ytimg.com", "i.imgur.com"],
  },
}

module.exports = nextConfig
